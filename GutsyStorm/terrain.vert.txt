uniform mat4 mvp;
varying vec4 vPos;
varying vec3 vNormal;

void main()
{
    vPos = gl_Vertex;
    vNormal = gl_Normal;

    gl_TexCoord[0] = gl_MultiTexCoord0;
    gl_FrontColor = gl_Color;
    gl_Position = mvp * gl_Vertex;
}