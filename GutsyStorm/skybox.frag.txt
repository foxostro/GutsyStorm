uniform samplerCube cubeMap;
varying vec3 vCubeMap;

void main (void)
{
    vec4 color = textureCube(cubeMap, vCubeMap);

    if(color.a < 0.5) {
		discard;
	}

	gl_FragColor = vec4(1.0, 0.1, 0.1, 1.0) * color;
}
