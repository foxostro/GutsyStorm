varying vec3 vCubeMap;

void main()
{
    vec3 vNormal = -gl_Normal.xyz;
    vec3 vView = -normalize(gl_Vertex.xyz);

    vCubeMap = normalize(reflect(vView, vNormal));

    gl_Position = gl_ModelViewProjectionMatrix * gl_Vertex;
}
